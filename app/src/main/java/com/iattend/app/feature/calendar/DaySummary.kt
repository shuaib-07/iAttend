package com.iattend.app.feature.calendar

/** Used by the Home tab's attendance heatmap (core/ui/AttendanceHeatmap.kt), not the Calendar tab. */
enum class DaySummary { NONE, PENDING, ALL_PRESENT, HAS_ABSENT }
